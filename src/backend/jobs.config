{
  "jobs": [
    {
      "functionLocation": "/wnbaCore.js",
      "functionName": "refreshReferenceData",
      "description": "Warm today's WNBA scoreboard into the WnbaCache collection so no visitor waits on ESPN.",
      "executionConfig": {
        "cronExpression": "0 * * * *"
      }
    }
  ]
}
